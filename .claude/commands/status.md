---
allowed-tools: Read, Bash(git status), Bash(pnpm run typecheck)
description: Show project status and next task
---
Show:
1. Git status (branch, uncommitted changes)
2. TypeScript errors (if any)
3. Current phase from CLAUDE.md
4. What to work on next
5. Test pass/fail summary
