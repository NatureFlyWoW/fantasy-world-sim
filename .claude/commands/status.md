---
allowed-tools: Read, Bash(git status), Bash(pnpm run typecheck), Bash(bash scripts/check-codebase-map.sh)
description: Show project status and next task
---
Show:
1. Git status (branch, uncommitted changes)
2. TypeScript errors (if any)
3. Codebase map check (`bash scripts/check-codebase-map.sh`)
4. Current phase from CLAUDE.md
5. What to work on next
6. Test pass/fail summary
