{
  "name": "@fws/electron",
  "version": "0.0.1",
  "private": true,
  "description": "Electron + PixiJS graphical frontend for Æternum",
  "type": "module",
  "main": "./dist/main/index.js",
  "scripts": {
    "dev": "tsc -p tsconfig.json && vite build && electron .",
    "build": "tsc -p tsconfig.json && vite build",
    "build:main": "tsc -p tsconfig.json",
    "build:renderer": "vite build",
    "typecheck": "tsc --noEmit",
    "start": "tsc -p tsconfig.json && vite build && electron ."
  },
  "dependencies": {
    "@fws/core": "workspace:*",
    "@fws/generator": "workspace:*",
    "@fws/narrative": "workspace:*",
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "vite": "^6.0.0"
  },
  "files": [
    "dist"
  ]
}
